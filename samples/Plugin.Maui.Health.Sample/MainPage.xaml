<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Plugin.Maui.Health.Sample.ViewModels"
             xmlns:charts="clr-namespace:Plugin.Maui.Health.Sample.Controls"
             xmlns:m2="clr-namespace:Plugin.Maui.Health.Sample.Models"
             x:Class="Plugin.Maui.Health.Sample.MainPage"
             x:DataType="vm:MainPageViewModel"
             BackgroundColor="{StaticResource PageBackground}"
             Title="Plugin.Maui.Health Sample">

    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="16">

            <!-- Header -->
            <VerticalStackLayout Spacing="2">
                <Label Text="Your health" Style="{StaticResource ScreenTitle}" />
                <Label Text="Powered by Plugin.Maui.Health" Style="{StaticResource MutedLabel}" />
            </VerticalStackLayout>

            <!-- Steps showcase: goal ring + weekly bar chart -->
            <Border Style="{StaticResource Card}">
                <VerticalStackLayout Spacing="16">
                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="18">
                        <charts:RingChartView Grid.Column="0"
                                              WidthRequest="110" HeightRequest="110"
                                              Progress="{Binding StepsGoalProgress}"
                                              ValueText="{Binding StepsGoalText}"
                                              Caption="Goal"
                                              RingColor="{StaticResource AccentGreen}" />
                        <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Center">
                            <Label Text="Steps today" Style="{StaticResource MetricCaption}" />
                            <Label Text="{Binding StepsTodayText}" Style="{StaticResource MetricValue}" />
                            <Label Text="Goal 10,000" Style="{StaticResource MutedLabel}" />
                        </VerticalStackLayout>
                    </Grid>
                    <Label Text="This week" Style="{StaticResource SectionHeader}" />
                    <charts:BarChartView Entries="{Binding StepsThisWeek}" BarColor="{StaticResource AccentCyan}" />
                </VerticalStackLayout>
            </Border>

            <!-- Explore -->
            <Label Text="Explore" Style="{StaticResource SectionHeader}" />
            <VerticalStackLayout BindableLayout.ItemsSource="{Binding MenuItems}" Spacing="12">
                <BindableLayout.ItemTemplate>
                    <DataTemplate x:DataType="m2:MenuItem">
                        <Border Style="{StaticResource Card}" Padding="16">
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer NumberOfTapsRequired="1"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:MainPageViewModel}}, Path=OpenViewCommand}"
                                    CommandParameter="{Binding .}" />
                            </Border.GestureRecognizers>
                            <Grid ColumnDefinitions="*,Auto" VerticalOptions="Center">
                                <Label Grid.Column="0" Text="{Binding Title}" FontSize="16" FontAttributes="Bold"
                                       TextColor="{StaticResource TextPrimary}" VerticalOptions="Center" />
                                <Label Grid.Column="1" Text="›" FontSize="26" TextColor="{StaticResource TextSecondary}" VerticalOptions="Center" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </BindableLayout.ItemTemplate>
            </VerticalStackLayout>

            <ActivityIndicator IsRunning="{Binding IsBusy}" Color="{StaticResource AccentBlue}" HorizontalOptions="Center" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
